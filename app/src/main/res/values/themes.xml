<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TinderCarApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/violeta</item>
    </style>

    <style name="Theme.TinderCarApp" parent="Base.Theme.TinderCarApp" />

    <style name="BottomNav">
        <item name="android:background">@color/violeta</item>
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/black</item>
    </style>
</resources>